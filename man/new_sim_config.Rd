% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{new_sim_config}
\alias{new_sim_config}
\title{SimConfig — Simulation Configuration S3 Class}
\usage{
new_sim_config(
  arrival_rate = 0.5,
  n_doctors = 3L,
  n_nurses = 5L,
  sim_duration = 480,
  urgency_probs = c(0.2, 0.3, 0.5),
  service_params = list(`1` = list(mean = 30, sd = 10), `2` = list(mean = 20, sd = 7),
    `3` = list(mean = 15, sd = 5)),
  seed = NULL
)
}
\arguments{
\item{arrival_rate}{Numeric. Average number of patient arrivals per time unit
(minute). Must be positive.}

\item{n_doctors}{Positive integer. Number of doctors available.}

\item{n_nurses}{Positive integer. Number of nurses available.}

\item{sim_duration}{Numeric. Total simulation duration in minutes. Must be
positive.}

\item{urgency_probs}{Numeric vector of length 3 summing to 1. Probabilities
of a patient being Critical, Urgent, or Standard respectively. Defaults to
\code{c(0.2, 0.3, 0.5)}.}

\item{service_params}{Named list with keys \code{"1"}, \code{"2"},
\code{"3"} (urgency levels). Each element is a list with \code{mean} and
\code{sd} for the service time distribution (minutes). Defaults to
clinically inspired values.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility.}
}
\value{
A \code{SimConfig} S3 object (a named list).
}
\description{
Constructs and validates a simulation configuration object used
to parameterise a \code{SimulationEngine} run.
Create a Simulation Configuration

Constructs a \code{SimConfig} S3 object containing all parameters needed to
run an ER simulation. All inputs are validated before the object is returned.
}
\examples{
cfg <- new_sim_config(
  arrival_rate = 0.5,
  n_doctors    = 3,
  n_nurses     = 5,
  sim_duration = 480
)
print(cfg)
}
